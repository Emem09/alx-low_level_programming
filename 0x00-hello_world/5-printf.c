#include <stdio.h>

/**
  * main - a program that that prints exactly
  * with proper grammar, but the outcome is a piece of art,followed by a nl
  * Return: 0 if executed correctly, else non zero
  */

int main(void)

{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
