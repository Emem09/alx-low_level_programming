#include <stdio.h>

/**
  * main - a program that printns the alphabet in lowercase
  * Return: 0 when successful
  */

int main(void)

{
	char ch;

	for (ch = 'a'; ch <= 'z '; ch++)
	
	{
		putchar(ch);

	}

	putchar('\n');

	return (0);
}
